package study.qa.lessonoop.cards.data;

public interface ICard {
    /*
    Nнтерфейс определяет набор публичных (public) методов для объектов, которые будут его имплементировать
     */
    void payInCountry(Country country, int amount);
}
